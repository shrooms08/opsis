{
  "case": "03-program-table-error",
  "covers": "SPL ATA failure, Custom 1 at top-level index 3 (meta.err is {InstructionError: [3, {Custom: 1}]}), so the failing program is ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL. The SPL ATA error table defines only code 0 (InvalidOwner), so code 1 is genuinely absent from the failing program's table: the resolver selects the namespace by failing program ID (Req 6.8) and then reports honestly rather than borrowing the System Program's row for the same numeric code (Req 6.3). Expected outcome is unresolved with reason not-in-table at raw confidence, and no attestation field at all, since nothing resolved (Req 6.10). That negative case is a sharper test of Req 6.3 than a successful lookup would be: the recorded logs show the ATA program invoking the System Program at depth 2 with 'Transfer: insufficient lamports 1588537, need 2039280' before failing with 0x1, so a range-matching implementation would confidently report ResultWithNegativeLamports here. Also the suite's only legacy message, exercising the non-v0 account resolution path.",
  "recordingNote": "The original covers prose asserted a resolution the ATA table cannot produce. It described code 1 as present in the System, SPL Token, and ATA tables simultaneously — and read a correct lookup as proof of program-ID namespace selection, with attestation program-id — when the AssociatedTokenAccountError enum declares a single variant, InvalidOwner, at code 0. The fixture is retained rather than replaced because the honest-degradation path it actually exercises is the more valuable one.",
  "cluster": "mainnet-beta",
  "recordedOn": "2026-08-15",
  "signature": "5xis1MD1MQXM7xtrCUESivrizTtfdq14B7huubfoaRM3FbcxWe6CN6wxW6G4m3DQmhWgUngFXMh63Sx3AChVJgkZ",
  "outcome": "failed"
}
