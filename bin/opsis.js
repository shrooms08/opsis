#!/usr/bin/env node
// Placeholder shim. Task 11.5 replaces this with the real one: import `main`
// from the built CLI, await it, and exit with the returned code. This file is
// the only place in Opsis that calls process.exit.
process.stderr.write('opsis: CLI not wired yet (see task 11.5)\n');
process.exit(70);
